// =============================================================
//  FILL THESE IN, then commit.  (Supabase → Project Settings → API)
//  The anon key is safe to expose in a public repo — access is
//  controlled by Row Level Security + login, not by hiding the key.
// =============================================================
window.APP_CONFIG = {
  SUPABASE_URL:      "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
